from agente_bibliotecario import AgenteBibliotecario
import event_store
import time def teste_direto(): print("[BOOST] Teste DIRETO do Bibliotec rio...") bib = AgenteBibliotecario() missao_id = f"TESTE_DIRETO_{int(time.time())}" print(f"[RADAR] Snapshot PRE na miss o {missao_id}...") res_pre = bib.executar({"modo": "pre_missao", "missao_id": missao_id}) print(f"Resultado PRE: {res_pre.get('status')}") print(f"[RADAR] Snapshot POS na miss o {missao_id}...") res_pos = bib.executar({"modo": "pos_missao", "missao_id": missao_id}) print(f"Resultado POS: {res_pos.get('status')}") # Conferir se criou os JSONs time.sleep(1) eventos = event_store.obter_cronologia(missao_id) print(f"Total de eventos registrados para {missao_id}: {len(eventos)}") for e in eventos: print(f" - Evento: {e['tipo']}") if __name__ == "__main__": teste_direto()
