import sys
import os
import time sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))) from src.agentes.ativos.bibliotecario_agente_SRC_GL import AgenteBibliotecario def log_gl(msg): prefix = f"[{time.strftime('%H:%M:%S')}] [[TEST] TESTE_BIBLIOTECARIO]" line = f"{prefix} {msg}" print(line) with open("ping_log_GL.txt", "a", encoding="utf-8") as f: f.write(line + "\n") def testar_integridade(): log_gl("Starting Teste do Pai do MCP...") b = AgenteBibliotecario() log_gl("1. Testando Modo Manuten o...") res = b.executar({"modo": "manutencao"}) log_gl(f"Resultado: {res}") log_gl("2. Testando Registro Pre-Mission...") res = b.executar({"modo": "pre_missao", "missao_id": "TESTE_SOTA_2026"}) log_gl(f"Resultado: {res}") if __name__ == "__main__": testar_integridade()
