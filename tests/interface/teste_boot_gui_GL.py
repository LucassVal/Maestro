import tkinter as tk
import os
import sys
import traceback def test_gui_boot(): print("[BOOST] Starting Teste de Boot da GUI GL...") try: # Adiciona a raiz ao path para garantir importa es base_dir = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))) sys.path.append(base_dir) sys.path.append(os.path.join(base_dir, "src", "interfaces")) sys.path.append(os.path.join(base_dir, "src", "core")) sys.path.append(os.path.join(base_dir, "src", "agentes", "passivos")) from src.interfaces.genio_gui_SRC_GL import GenioGUI root = tk.Tk() # root.withdraw() # N o abre a janela fisicamente no Windows # Simula cria o da interface app = GenioGUI(root) # D um tempinho para a UI processar (sem entrar no mainloop) root.update_idletasks() root.update() print("[OK] GUI instanciada e atualizada com sucesso.") root.destroy() return True except Exception as e: print("[ERR] FALHA NO BOOT DA GUI:") traceback.print_exc() return False if __name__ == "__main__": success = test_gui_boot() sys.exit(0 if success else 1)
